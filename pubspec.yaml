name: aya_share
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  quran: ^1.3.3
  # english_words: ^4.0.0
  provider: ^6.0.3
  # google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Removed google_fonts from dev_dependencies as it is already in dependencies 
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

